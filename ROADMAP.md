# Roadmap / ideas

Comment on (or open) an issue first so we don't duplicate work.

## Good first issues
- [ ] Add screenshots of Nerd Mode to the README
- [ ] DevSetup: move the list of packages into a `packages.json` so people can edit it without touching code
- [ ] DevSetup: add a `-WhatIf` / dry-run mode
- [ ] Nerd Mode: add a light theme and a "copy report to clipboard" button

## Security
- [ ] Elevated Context Menu: lock the launcher folder down with ACLs (Administrators-only write) and add an optional allow-list of script folders

## Features
- [ ] Nerd Mode: GPU and temperature sensors, per-process network usage
- [ ] Nerd Mode: a matching Python/Linux companion that emits the same JSON schema
- [ ] Publish the tools to the PowerShell Gallery
